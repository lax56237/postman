name : postman-clone
services:
  db:
    image: postgres:latest
    environment:
     - POSTGRES_USER=postgres
     - POSTGRES_PASSWORD=2005L@xpostgres
     - POSTGRES_DB=postgres
    ports:
      - 5432:5432